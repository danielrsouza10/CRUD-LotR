sap.ui.define(["sap/ui/core/ComponentContainer"], (ComponentContainer) => {
  "use strict";

  new ComponentContainer({
    name: "ui5.o_senhor_dos_aneis",
    settings: {
      id: "o_senhor_dos_aneis",
    },
    async: true,
  }).placeAt("content");
});
